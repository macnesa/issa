{
  "name": "issa-server",
  "version": "1.0.0",
  "description": "Integrated Service of School Activity",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "reset-db": "sequelize-cli db:drop && sequelize-cli db:create && sequelize-cli db:migrate && sequelize-cli db:seed:all",
    "test": "jest --runInBand --forceExit --coverage",
    "reset-server-test": "sequelize-cli db:drop --env test && sequelize-cli db:create --env test && sequelize-cli db:migrate --env test && sequelize-cli db:seed:all --env test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MashuOU/ISSA-server.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/MashuOU/ISSA-server/issues"
  },
  "homepage": "https://github.com/MashuOU/ISSA-server#readme",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jest": "^29.5.0",
    "jsonwebtoken": "^9.0.0",
    "midtrans-client": "^1.3.1",
    "nodemailer": "^6.9.1",
    "pg": "^8.9.0",
    "postgres": "^3.3.3",
    "sequelize": "^6.29.0",
    "slug": "^8.2.2",
    "socket.io": "^4.6.1",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
